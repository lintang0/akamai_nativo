# `ContentEdgeAkamaiApp`

This is the Nativo Edge full app for Akamai.

install dependencies with npm,

```bash
npm install
```

To build the project, please run

```bash
# with npm
npm run build

The built files is located at `/dist` directory.
